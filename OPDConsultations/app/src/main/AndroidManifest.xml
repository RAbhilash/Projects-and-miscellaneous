<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.opdconsultations">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/snake"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Reports"></activity>
        <activity android:name=".PaymentActivity" />
        <activity android:name=".appointment_activities.AppointmentInfo" />
        <activity
            android:name=".PhysicianActivity"
            android:label="Physician Profile" />
        <activity
            android:name=".AppointmentActivity"
            android:label="Appointments" />
        <activity
            android:name=".AboutApp"
            android:label="About App"
            android:parentActivityName=".MainPage" />
        <activity
            android:name=".Emergency"
            android:label="Emergency"
            android:parentActivityName=".MainPage" />
        <activity
            android:name=".SettingPage"
            android:label="Settings"
            android:parentActivityName=".MainPage" />
        <activity
            android:name=".SignUpPage"
            android:label="SignUp" />
        <activity
            android:name=".LoginPage"
            android:label="OPD Appointment">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainPage"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />

        <meta-data
            android:name="com.razorpay.ApiKey"
            android:value="rzp_test_mLrIgNtx1HgeGa" />
    </application>

</manifest>